.发布前收集流程是否存在汇聚节点；

通过

.发布前收集所有汇聚节点的可达路径列表；

暂时使用hashmap存储路径映射，key为汇聚节点的id，value为list对象，list中每个元素是路径中每个连线的唯一标识id

通过

.发布前初始化每条连线分支是否存在影响的所有汇聚节点（相比运行态去判断能提高性能效率）；

遍历连线的id是否存在于hashmap中某一个value列表中
(只需要获取一个bool标识来确定是否存在影响汇聚节点)

通过

.流程启动时，如果存在汇聚节点在实例中初始化所有汇聚节点的可达路径列表；
直接clone一份

通过

.如果分支被拒绝执行，无论普通的分支（等同xor）还是网关节点的条件or，都需要在实例中去除所有的汇聚可达路径；
如果连线分支被拒绝（这里包括未执行的），则判断连线boo标识是否需要清除
如果分支的目的节点就是汇聚，则清除汇聚节点所有可达路径是当前的分支的路径列表；

.当汇聚节点到达激活时，判断可达路径列表（未执行路径）是否清空，如果情空则继续执行，否则需要返回等待；




